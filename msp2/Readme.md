# Upper body needs a bit more refinement
/*Note: to update the package directory as per the requirements of the package installation file*/

// Pending works:
   1.) Collisions evalution has to be done for further moveit setup
   2.) After moveit, lower body also has to be evaluated
   